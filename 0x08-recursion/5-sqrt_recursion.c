/**
 * _sqrt_recursion - return the natural square root of a number
 * @n: number
 * Return: natutal square root of n, -1 if n does not have
 */
int _sqrt_recursion(int n)
{
	if (n < 1)
		return (-1);
	if (n == 1)
		return (1);
}
